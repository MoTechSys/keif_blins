# سجل التغييرات — كيف الضيافة

## [2.2.0+4] — 2026-09-05

> إصدار المستندات الرسمية: فاتورة/عرض سعر بثيم رسمي موحّد، كشف حساب مختصر كلاسيكي، **كشف حساب تفصيلي جديد** يعكس كل فاتورة بالكامل، وسند قبض نصف صفحة. البنود التفصيلية أدناه (كانت تحت «غير منشور»)، ويليها قسم «2.2.0 — قيد العمل» الذي يحوي بقية أعمال هذا الإصدار وحالة ملاحظات المستخدم.

### مراجعة صفرية للاستقرار والحجم (بعد الإصدار الأول لـ 2.2.0+4)
- **إصلاح انهيار محتمل في الكشف التفصيلي:** كانت البطاقة القصيرة تُعامل ككتلة «لا تنقسم» بعدّ البنود (≤ 6) لا بارتفاعها؛ فاتورة بستة بنود ذات وصف طويل، أو فاتورة عليها عشرات الدفعات، أو عشرات الدفعات على الحساب — كل منها كانت تُسقط المولّد بخطأ `Widget won't fit into the page` فتظهر للمستخدم رسالة خطأ بدل الكشف. الآن يُقدَّر ارتفاع البطاقة بالنقاط (وصف متعدد الأسطر، التفاف، دفعات، إجماليات، ملاحظات) ولا تبقى كتلة واحدة إلا إن كانت أقل من نصف صفحة؛ وعند كثرة الدفعات (> 14 صفًا) يُرسم جدول الدفعات بعرض البطاقة كجدول قابل للانقسام بتكرار رأسه، وتبقى الإجماليات كتلة مستقلة. جدول «دفعات على الحساب» صار قابلًا للانقسام كذلك. اختبار ضغط جديد `pdf_stress_test.dart` (6 بنود × 8 أسطر، 60 دفعة على فاتورة، 60 دفعة على الحساب، فاتورة 60 بندًا + عرض + سند بنصوص طويلة، بيانات فارغة/صفرية) — 32 اختبارًا ناجحًا.
- **تحليل الحجم (28 م.ب):** الحزمة الشاملة تحمل محرك Flutter وكود التطبيق **ثلاث مرات** (arm64 + armv7 + x86_64 ≈ 9 م.ب لكل معمارية) — هذا أصل الحجم لا محتوى التطبيق؛ أصول التطبيق كلها < 1 م.ب. لذلك تُنشر حزم لكل معمارية (arm64 ≈ 10.9 م.ب وهي المناسبة لكل الهواتف الحديثة، armv7 ≈ 10.7 م.ب للأجهزة القديمة) مع الشاملة لمن لا يعرف معماريته.
- تقليص ما يمكن تقليصه فعلًا: حذف `assets/img/mark.png` (116 ك.ب، غير مستخدم في أي مكان)، تصغير أيقونات الواجهة من 288px إلى 160px (703 → 375 ك.ب؛ أكبر عرض معروض 46dp)، إزالة حزمة `intl` غير المستخدمة، وإضافة ملف `proguard-rules.pro` المفقود الذي يشير إليه Gradle.
- مراجعة مسارات الانهيار في التطبيق: كل توليد PDF يمر عبر `PreviewScreen`/`runBusy` بمعالجة أخطاء تعرض رسالة بدل الانهيار؛ التهيئة (`Store.init`/`LockService.init`) محمية بـ try/catch مع شاشة إعادة المحاولة؛ سجلات التخزين التالفة تُتجاوز فردًا فردًا؛ كل `int.parse/DateTime.parse` محمية أو `tryParse`؛ الوصول إلى `.first` مشروط بعدم الفراغ؛ خرائط تسميات الحالة تغطي كل قيم التعداد؛ الدخول بحساب Google محاط بـ try/catch مع بديل «المتابعة بدون تسجيل».

### القائمة الجانبية: «الملفات المحفوظة»
- ربط شاشة `FilesScreen` (كانت موجودة منذ 2.1.0 لكنها غير موصولة من أي مكان بعد إعادة تصميم القائمة) ببند «الملفات المحفوظة» في القائمة الجانبية، يظهر على الهاتف فقط (لا على الويب حيث لا مجلد محلي). تعرض ملفات PDF والنسخ الاحتياطية في مجلد الهاتف مصنّفة حسب النوع مع مشاركة/فتح/حذف.

### الكشف التفصيلي: تحقق داخلي واختبارات حالات حرجة
- مجاميع ملخص الكشف التفصيلي (العرابين، الدفعات على الفواتير، دفعات على الحساب) مقيّدة بالفترة المختارة كي تساوي دائمًا معادلة الكشف (`buildStatement`)؛ بطاقة الفاتورة نفسها تعرض كل دفعاتها لتعكس حالتها الفعلية حتى لو كانت دفعة بعد نهاية الفترة. تحقق داخلي (`assert`) يضمن: مجموع البطاقات = إجمالي الفواتير، مجموع المدفوعات المعروضة = إجمالي المدفوعات، عدّادات الحالة = عدد الفواتير.
- اختبار جديد يغطي: دفعة بتاريخ بعد الفترة، فترة بلا فواتير، فاتورة من 40 بندًا بوصف متعدد الأسطر (تنقسم على 4 صفحات بلا خطأ «Widget won't fit» مع بقاء الملخص متصلًا — `statement_detailed_long.pdf`)، وفاتورة مدفوعة بالزيادة (رصيد دائن).

### كشف الحساب: العودة إلى الهيكلية الكلاسيكية (مدين/دائن/رصيد) بالهوية الرسمية
- **تحليل:** التصميم الرسمي السابق حوّل الكشف إلى «إجمالي فواتير لفترة» (سعر × فترات ومجموع) تبعًا للنموذج المرجعي، فضاعت المدفوعات والرصيد المتجدد. كشف الحساب المتعارف عليه محاسبيًا هو سجل حركة الحساب: التاريخ | المرجع | البيان | **مدين** (فواتير) | **دائن** (دفعات) | **الرصيد**، يبدأ برصيد افتتاحي وينتهي بالرصيد المستحق — وهو ما كان في الإصدار 2.1.0.
- **التنفيذ:** أُعيدت الهيكلية القديمة (بيانات العميل/الكشف، جدول واحد للحركات، سطر إجمالي، ملخص: إجمالي الفواتير − إجمالي المدفوعات = **الرصيد المستحق** بالتفقيط، ملاحظة السداد) لكن بالترويسة والألوان والجدول الرسمي الموحّد مع الفاتورة. الشارة في الترويسة تعرض «مستحق: …» أو «لا مستحقات».
- **قراءة أسهل (بطلب المستخدم):** تظليل متناوب للصفوف (سطر وسطر)، أرقام **المدين بالأحمر** و**الدائن/المدفوعات بالأخضر** في الصفوف وسطر الإجمالي، والرصيد بالخط الغامق.
- **حُذف سطر «رصيد افتتاحي»** من الكشف والملخص؛ الكشف يعرض حركات الفواتير والمدفوعات فقط ويبدأ الرصيد من أول حركة. استثناء واحد: عند تصفية الكشف على فترة محددة وكان للعميل حركات قبلها، يظهر سطر «رصيد سابق قبل الفترة» (بخلفية مميزة) كي يستقيم الرصيد المتجدد — ولا يظهر أبدًا إن كان صفرًا.
- الرصيد الدائن (لصالح العميل) يُعرض بين قوسين في الجدول وبعنوان «رصيد دائن للعميل» في الملخص.
- رسالة واتساب المرافقة للكشف تعرض الرصيد السابق (إن وُجد) ونفس معادلة الملخص.
- اختبارات: الرصيد الختامي = رصيد افتتاحي + فواتير − مدفوعات، وترحيل حركات ما قبل الفترة كرصيد سابق (`statement_period.pdf`).

### كشف الحساب المختصر: خط أوضح وأكبر
- تكبير خط الجدول (الرؤوس 10، الخلايا 9.8، سطر الإجمالي 10.2)، وبيانات العميل/الكشف 10.4، والرصيد المستحق 14.4، والتفقيط 11.2، مع إعادة موازنة عرض الأعمدة (تقليص م/التاريخ/المرجع/المبالغ) كي يتّسع «البيان» لسطر واحد دون التفاف.

### جديد: كشف حساب تفصيلي (مرآة كاملة لكل فاتورة مع دفعاتها)
- عند إنشاء كشف الحساب يختار المستخدم النوع: **مختصر** (مدين / دائن / رصيد) أو **تفصيلي**. اسم الملف يُميّز النوع («كشف حساب تفصيلي - العميل - التاريخ.pdf»).
- **مبدأ الاكتمال:** كل ما يظهر في الفاتورة يظهر في الكشف التفصيلي بنفس المنطق والمسميات، بلا اختصار ولا اختراع بيانات:
  - **شريط عنوان الفاتورة** (خلفية رسمية): الرقم التسلسلي، رقم الفاتورة، التاريخ، وشارة الحالة على اليسار (مدفوعة أخضر / مدفوعة جزئيًا ذهبي / غير مدفوعة أحمر).
  - **سطر بيانات الفعالية** (يظهر فقط عند وجودها): تاريخ الفعالية (أو مدتها من–إلى)، الموقع، عدد الحضور.
  - **جدول البنود** بنفس أعمدة الفاتورة: م | الوصف **كاملًا متعدد الأسطر** (السطر الأول عريض والتفاصيل التالية بشرطات) | رأس الكمية الذكي حسب الوحدة (عدد الأيام / عدد الفترات / الساعات / الأشخاص / الوجبات، أو «الكمية» مع كتابة الوحدة بجانب الرقم عند اختلاف الوحدات) | السعر | **مشتريات خارجية** (العمود يظهر فقط إن وُجدت مشتريات في الفاتورة، و«—» للبند بلا مشتريات) | الإجمالي؛ تظليل متناوب وحدود داخلية واضحة.
  - **الدفعات المستلمة على الفاتورة** (يمين): التاريخ | السند | الطريقة | المبلغ، ويظهر **العربون** كأول صف بتاريخ الفاتورة، ثم كل دفعة برقم سندها (أو مرجعها)، ثم صف «الإجمالي». عند عدم وجود أي دفعة تظهر عبارة «لم تُستلم أي دفعة على هذه الفاتورة حتى تاريخ الكشف» بالأحمر.
  - **إجماليات الفاتورة** (يسار، داخل إطار): خدمات الضيافة + مشتريات خارجية (عند وجود مشتريات)، المجموع (عند وجود خصم أو ضريبة)، الخصم، ضريبة القيمة المضافة بنسبتها — تختفي تلقائيًا عند عدم تفعيلها — ثم **إجمالي الفاتورة** عريض، العربون والدفعات بالأخضر، و**المتبقي** بالأحمر (أو «مدفوع بالزيادة» بالأخضر).
  - **ملاحظات الفاتورة** في شريط أسفل الكتلة إن وُجدت.
- قسم «دفعات على الحساب (غير مخصصة لفاتورة محددة)» يظهر فقط عند وجودها (م | التاريخ | السند | الطريقة | المرجع / ملاحظات | المبلغ) مع إجماليها في شريط العنوان.
- **ملخص الحساب** في بطاقة واحدة: يمين عدّادات الحالة (عدد الفواتير خلال الفترة، مدفوعة بالكامل، مدفوعة جزئيًا، غير مدفوعة بألوانها)، ويسار المبالغ (رصيد سابق قبل الفترة إن وُجد، إجمالي الفواتير، العرابين المستلمة، الدفعات على الفواتير، دفعات على الحساب — كل منها إن وُجد — ثم إجمالي المدفوعات) وأسفلها **الرصيد المستحق** بخط كبير مع التفقيط، ثم ملاحظة السداد وتذييل الحساب البنكي والختم.
- **تقسيم الصفحات بدقة:** كتلة الفاتورة القصيرة (≤ 6 بنود ودفعات) لا تنقسم أبدًا بين صفحتين؛ الفاتورة الطويلة تُرسم بوضع منقسم: العنوان مع أول البنود، جدول البنود ينقسم بتكرار رؤوس الأعمدة وبحدود جانبية متصلة، ثم كتلة الدفعات/الإجماليات لا تنفصل عن بعضها. كتلة الملخص والتذييل لا تنقسم كذلك.
- الترويسة: شارة «كشف حساب تفصيلي» + «مستحق: …»، وترقيم الصفحات يحمل رقم الكشف واسم العميل. رسالة واتساب المرافقة هي نفس رسالة الكشف.
- اختبارات: بيانات الكشف تتضمن فواتير بمشتريات خارجية وعربون ووصف متعدد الأسطر ووحدات مختلطة وملاحظات وفاتورة طويلة (9 بنود) للتحقق من وضع الانقسام؛ عناصر الثيم الجديدة: `innerBorder`, `cardDeco`, `cardHeader`, و`officialTable(border:)`.

### سند القبض: تصميم رسمي نصف صفحة (A5 عرضي) جديد
- **تحليل:** التصميم السابق كان صفحة A4 كاملة بالثيم الملكي القديم غير الموحّد مع بقية المستندات، ولا يعكس الشكل المتعارف عليه لسند القبض الورقي. المرجعان المعتمدان: سند نصف صفحة بحقول منقّطة وخانات اختيار لطريقة الدفع وتوقيعات.
- **التنفيذ:** صفحة **A5 عرضية** (نصف A4) بالهوية الرسمية: ترويسة (شعار، المملكة، اسم المؤسسة، س-ت، الرقم الضريبي) مع شارة بنية «سند قبض / RECEIPT VOUCHER»، خط مزدوج، صندوقا **رقم السند** و**التاريخ**، حقول منقّطة: «استلمنا من السيد/ة» + «رقم الجوال»، «وذلك مقابل» (رقم الفاتورة والفعالية والموقع، أو «دفعة على الحساب»)، صندوق **مبلغ وقدره** بخلفية مميزة والمبلغ داخل إطار ذهبي بمنزلتين عشريتين + التفقيط (حسب الإعداد)، شريط **طريقة الدفع** بخانات اختيار (نقدًا / شبكة / تحويل / شيك / أخرى أو الطريقة المخصصة) و**الحالة** (دفعة / سداد كامل / على الحساب)، سطر **رقم العملية / المرجع** و**ملاحظات** عند وجودهما، وسطر حساب الفاتورة: **إجمالي الفاتورة | المدفوع سابقًا | هذه الدفعة (أخضر) | المتبقي بعد الدفعة (أحمر، أو أخضر عند السداد الكامل)**.
- أسفل السند: **توقيع المستلم | ختم المؤسسة | توقيع العميل** (خطوط منقّطة، تُخفى حسب إعدادات التوقيع/الختم)، ثم شريط تذييل بني بالبريد والموقع والجوال. علامة مائية بالشعار بشفافية خفيفة.
- **إصلاح حسابي:** «المتبقي» في السند كان يتجاهل الدفعات الأخرى على نفس الفاتورة؛ الآن يُحسب من إجمالي الفاتورة − العربون − الدفعات السابقة (بتاريخ أقدم أو نفس التاريخ وأسبق إنشاءً) − هذه الدفعة. الواجهات تمرر قائمة الدفعات (`receipt(..., payments:)`).
- القيم اللاتينية (الجوال، المرجع) داخل الحقول المنقّطة تلتصق بعنوانها بدل الانزلاق إلى أقصى اليسار.
- اختبارات: سند لفاتورة (مع دفعات سابقة) وسند «على الحساب» (`receipt_account.pdf`)، صفحة واحدة لكل منهما.

### شفافية الأرقام في كشف الحساب
- **إصلاح:** كان الإجمالي في كشف الحساب يظهر أكبر من السعر (مثل 12,000 → 13,800) دون تفسير، لأن الفواتير كانت تُنشأ بضريبة 15% افتراضيًا حتى مع إيقاف الضريبة في الإعدادات (القيمة الافتراضية للنموذج وللقراءة من التخزين). الافتراضي الآن **0 = بدون ضريبة**، وتُطبَّق الضريبة فقط عند تفعيلها في الإعدادات أو اختيارها في الفاتورة.
- (تجاوزه لاحقًا في هذا الإصدار) أُضيفت مؤقتًا أعمدة الخصم/الضريبة لجدول تفصيل الفواتير في الكشف؛ ثم أُلغي هذا الجدول كليًا مع العودة للهيكلية الكلاسيكية أعلاه. تفصيل السعر/الخصم/الضريبة يبقى في الفاتورة نفسها.

### مطابقة الاتجاهات والجداول مع النماذج الرسمية
- كتلة الإجماليات (خدمات الضيافة/المجموع/الخصم/الضريبة/الإجمالي/الصافي/المتبقي) تُثبَّت الآن على **يسار الصفحة** في الفاتورة وعرض السعر كما في النموذج الرسمي، وليس على اليمين.
- كشف الحساب: ملخص الحساب و«الرصيد المستحق» والتفقيط في كتلة يسارية واحدة (التسميات قرب المنتصف والقيم عند الهامش الأيسر) مع خط فاصل بعرض الصفحة قبل التذييل.
- إضافة عمود «م» للترقيم (1، 2، 3…) كأول عمود من اليمين في جدول بنود الفاتورة وعرض السعر.
- حدود الجدول أوضح: إطار خارجي 1.1pt وخطوط داخلية للصفوف والأعمدة 0.85pt بلون أغمق قليلاً (#B5A386).

### التصميم الرسمي البسيط للمستندات (الملاحظة 7 — «غيّر»)
- **الفاتورة / عرض السعر** (`lib/pdf/official_theme.dart`, `documents.dart`): إعادة تصميم كامل على نسق ملف «عرض الفاتورة – كيف الضيافة»: ترويسة (شعار يمين، اسم المؤسسة وسط، شارة «فاتورة»/«عرض سعر» يسار)، خط مزدوج، بيانات العميل والفعالية يمينًا وبيانات المستند يسارًا، جدول بنود بخلفية `#f0e3cc`، ملخص مالي يسار، تذييل (الختم يمين / البنك وIBAN والتواصل يسار / شريط شعارات الجهات) وترقيم صفحات ذهبي.
- العنوان «فاتورة» لا «فاتورة ضريبية»؛ حُذفت التواقيع والشروط والإقرار والتفقيط من الفاتورة (يبقى الختم والعلامة المائية حسب الإعدادات).
- سطرا الضريبة والخصم يختفيان كليًا عند عدم التفعيل؛ عمود «مشتريات خارجية» يظهر فقط عند وجود مشتريات؛ عنوان عمود الكمية يتبع الوحدة (عدد الأيام/الفترات/الأشخاص…).
- **كشف الحساب:** (صيغة «تفصيل فواتير» أُلغيت لاحقًا في هذا الإصدار — انظر أعلاه) تبقّى منها: الترويسة الرسمية مع تكرارها على كل صفحة، وزخرفة الأوراق أعلى/أسفل الصفحة (`assets/img/leaf-tile.svg`)، وحقل `Statement.invoices` في `models.dart`.
- شريط شعارات الجهات (`assets/img/certs.png`) في تذييل الفاتورة.

### إصلاحات عرض النص العربي في PDF
- **مسافات مفقودة بعد «ر» و«ز»** («نورسعيد» بدل «نور سعيد»): سببها أن حزمة `pdf` تُموضع كلمات RTL بعرض الحبر بدل عرض التقدّم، وحرفا ر/ز في Tajawal لهما `xMin` سالب. أُدرجت نسخة محلية معدّلة `third_party/pdf` (3.12.0) عبر `dependency_overrides` مع توثيق التعديل في `third_party/pdf/KEIF_PATCH.md`.
- المبالغ «15,000.00 ر.س» تُعرض بترتيب صحيح (لم تُعد تُجبر LTR)، وسطر `IBAN: SA…` نص واحد LTR في التذييل.

### حجم الملف
- صور الشعار والختم للـ PDF بنسخ مصغّرة مُكمَّمة (`assets/img/pdf/`) — حجم الفاتورة من ~149 KB إلى ~105 KB.

### مستندات PDF (الملاحظات 6، 7أ، 9، 11أ)
- **إصلاح اتجاه الجداول (RTL):** حزمة `pdf` تتجاهل `textDirection` في `pw.Table`؛ أُضيف المساعد `rtlTable()` في `pdf_theme.dart` يعكس ترتيب الخلايا ويعيد ربط عروض الأعمدة. الآن عمود «م» في أقصى اليمين في الفاتورة/عرض السعر/كشف الحساب.
- **اتجاه العناصر داخل المستند:** الأرقام، المبالغ، التواريخ، أرقام الفواتير وIBAN تُعرض LTR داخل نص RTL (لا تنقلب).
- **تذييل مقروء:** سطر بنكي واضح (رقم الحساب لدى البنك + IBAN بخط 9.6pt) ثم سطر التواصل (الهاتف/الموقع/البريد/س.ت) بخط 8–8.8pt بدل الخط الصغير السابق.
- **احترام «عناصر المستند» (إعدادات الفواتير):** ما لم يُفعَّل لا يظهر في PDF: البنك، الرقم الضريبي، س.ت، الشروط، التواقيع/الختم، التفقيط، إقرار الاستلام، المدفوع/المتبقي، بيانات المناسبة، العلامة المائية.
- سطر «ضريبة القيمة المضافة» يُخفى عند نسبة 0% (لا مزيد من «(0%)»).
- التصميم الحالي مُحتفظ به بعد إصلاح الاتجاه بانتظار قرار «غيّر» لتطبيق التصميم الرسمي (الملاحظة 7).

### واجهة
- ترحيل `RadioListTile` إلى `RadioGroup` (Flutter 3.35) في إعدادات الترقيم، وتنظيف تحذيرات المحلل (`flutter analyze`: لا مشاكل).

## [2.2.0] — قيد العمل (فرع main)

### ملاحظات المستخدم (13 ملاحظة) وحالتها

| # | الملاحظة | الحالة |
|---|----------|--------|
| 1 | حذف بطاقة «أهلًا بك في كيف الضيافة» من الرئيسية | ✅ منجز |
| 2 | شاشة الدخول: نص الشعار أبيض على الوضع الداكن (`assets/img/logo_light.png`) | ✅ منجز |
| 3 | بطاقة الرئيسية بلا أثر دلّة (توهج شعاعي فقط) — تصميم المزدحمة مقترح فقط | ✅ مؤكَّد |
| 4 | قائمة جانبية قصيرة، الإعدادات في النهاية، صفحة «إعدادات الفواتير» مستقلة، حول التطبيق (معين العباسي / +967770941666 / alabbasi.uk) | ⏳ قادم |
| 5 | ترتيب دورة العمل: إنشاء عميل → فاتورة → دفعة → كشف حساب؛ «أحدث الفواتير» 3 صفوف + زر «عرض كل الفواتير (N)» | ✅ منجز |
| 6 | فاتورة PDF تُعرض LTR بدل RTL (عكس ترتيب أعمدة `pw.Table`) | ⏳ قادم |
| 7 | ترتيب العمل على المستندات: إصلاح RTL أولًا → عرض على المستخدم → ثم تصاميمه (سند القبض النصفي بعد الموافقة) | ⏳ قادم |
| 8 | صفحة العميل: قوائم مختصرة (4) + «عرض الكل (N)» بتبويبات/بحث/تصفية؛ الرصيد السالب يُعرض «رصيد دائن للعميل»؛ سلة محذوفات 30 يومًا | ✅ الصفحة + المخزن — ⏳ واجهة السلة |
| 9 | خطأ «إجمالي العرض 0.00» (`clamp(0, 1<<62)` يعطي 0 على dart2js) | ✅ منجز ومُتحقَّق |
| 10 | عروض أسعار لغير العملاء (عرض سريع باسم وجوال)، التحويل لفاتورة يُنشئ عميلًا من بيانات العرض، صفحة العميل تعرض عروضه | ✅ منجز (بقي قسم مستقل في القائمة) |
| 11أ | مفاتيح إظهار عناصر المستندات (بنك/IBAN، رقم ضريبي، س.ت، شروط، توقيعات، تفقيط، إقرار، متبقي…) | ✅ الحقول في `Org` — ⏳ الواجهة + PDF |
| 11ب | نسخة احتياطية بنمط واتساب: اسم `keif-backup-YYYY-MM-DD_HH-mm.json`، قائمة النسخ، استرجاع آخر نسخة، حذف اللصق، زر Google | ✅ التسمية — ⏳ الواجهة |
| 11ج | أنماط الترقيم: تسلسلي / تاريخ ووقت `INV-20260509-143522` / بادئة السنة | ✅ المخزن (`nextNumber`, `previewNumber`) — ⏳ الواجهة |
| 11د | أسماء الملفات: `فاتورة INV-0005 - العميل.pdf`، `عرض سعر QT-0001 - العميل.pdf`، `سند قبض REC-0001 - العميل.pdf`، `كشف حساب - العميل - التاريخ.pdf`، `إشعار تسليم - …` | ✅ منجز |
| 12 | شريط التنقل كما هو؛ مقترح استبدال «الكشوف» — لا يُنفَّذ إلا بموافقة | ⏸ بانتظار القرار |
| 13 | صفحة العميل مرتبة وغير مزدحمة | ✅ منجز |

### تفاصيل التنفيذ
- **models.dart**: `Totals` صحيحة بدون `clamp`؛ `Invoice.quickPhone/deletedAt/isQuick/isDeleted`؛ `Payment/Client.deletedAt`؛ `Org.numberingMode/numberYear` + 9 مفاتيح `show*`.
- **store.dart**: حذف ناعم (`deletedAt`) مع قوائم سلة، `purgeExpiredTrash()` تلقائي عند التشغيل، `restore*/purge*/emptyTrash/daysLeft`، `nextNumber` بثلاثة أنماط، `previewNumber`، `convertQuote(q, clientId:)`، مخطط تصدير 3 مع `counts`، دوال أسماء الملفات الموحّدة.
- **doc_form.dart**: مفتاح «عميل مسجّل / عرض سريع» مع حقلي الاسم والجوال؛ لا يُختار أول عميل تلقائيًا.
- **doc_detail.dart**: تحويل العرض السريع يفتح نموذج عميل مملوءًا مسبقًا ثم يُنشئ الفاتورة؛ صف الضريبة يظهر فقط عند وجود ضريبة؛ الحذف ينقل إلى السلة.
- **clients_screen.dart**: بطاقة رصيد مع شارة «N غير مسدَّدة»، بيانات التواصل مطوية، 4 أزرار بترتيب دورة العمل، أقسام مختصرة، `ClientLedgerScreen` (تبويبات + بحث + تصفية بالحالة).
- **docs_screen.dart**: زر «عرض سعر جديد» لا يشترط وجود عملاء.
- **home_screen.dart / signin_screen.dart**: حسب الملاحظات 1، 2، 5.

### المتبقي للإصدار 2.2.0+4
1. إعادة بناء القائمة الجانبية + مركز الإعدادات + صفحة إعدادات الفواتير (المفاتيح والترقيم بمعاينة حيّة) + حول التطبيق.
2. شاشة سلة المحذوفات.
3. شاشة النسخ الاحتياطية بنمط واتساب.
4. PDF: احترام المفاتيح، إصلاح RTL للجداول، تذييل مقروء، تسمية إشعار التسليم → عرض عينات للمستخدم.
5. رفع رقم الإصدار وبناء APK وإصدار GitHub.
