# سجل التغييرات — كيف الضيافة

## [2.2.0] — قيد العمل (فرع main)

### ملاحظات المستخدم (13 ملاحظة) وحالتها

| # | الملاحظة | الحالة |
|---|----------|--------|
| 1 | حذف بطاقة «أهلًا بك في كيف الضيافة» من الرئيسية | ✅ منجز |
| 2 | شاشة الدخول: نص الشعار أبيض على الوضع الداكن (`assets/img/logo_light.png`) | ✅ منجز |
| 3 | بطاقة الرئيسية بلا أثر دلّة (توهج شعاعي فقط) — تصميم المزدحمة مقترح فقط | ✅ مؤكَّد |
| 4 | قائمة جانبية قصيرة، الإعدادات في النهاية، صفحة «إعدادات الفواتير» مستقلة، حول التطبيق (معين العباسي / +967770941666 / alabbasi.uk) | ⏳ قادم |
| 5 | ترتيب دورة العمل: إنشاء عميل → فاتورة → دفعة → كشف حساب؛ «أحدث الفواتير» 3 صفوف + زر «عرض كل الفواتير (N)» | ✅ منجز |
| 6 | فاتورة PDF تُعرض LTR بدل RTL (عكس ترتيب أعمدة `pw.Table`) | ⏳ قادم |
| 7 | ترتيب العمل على المستندات: إصلاح RTL أولًا → عرض على المستخدم → ثم تصاميمه (سند القبض النصفي بعد الموافقة) | ⏳ قادم |
| 8 | صفحة العميل: قوائم مختصرة (4) + «عرض الكل (N)» بتبويبات/بحث/تصفية؛ الرصيد السالب يُعرض «رصيد دائن للعميل»؛ سلة محذوفات 30 يومًا | ✅ الصفحة + المخزن — ⏳ واجهة السلة |
| 9 | خطأ «إجمالي العرض 0.00» (`clamp(0, 1<<62)` يعطي 0 على dart2js) | ✅ منجز ومُتحقَّق |
| 10 | عروض أسعار لغير العملاء (عرض سريع باسم وجوال)، التحويل لفاتورة يُنشئ عميلًا من بيانات العرض، صفحة العميل تعرض عروضه | ✅ منجز (بقي قسم مستقل في القائمة) |
| 11أ | مفاتيح إظهار عناصر المستندات (بنك/IBAN، رقم ضريبي، س.ت، شروط، توقيعات، تفقيط، إقرار، متبقي…) | ✅ الحقول في `Org` — ⏳ الواجهة + PDF |
| 11ب | نسخة احتياطية بنمط واتساب: اسم `keif-backup-YYYY-MM-DD_HH-mm.json`، قائمة النسخ، استرجاع آخر نسخة، حذف اللصق، زر Google | ✅ التسمية — ⏳ الواجهة |
| 11ج | أنماط الترقيم: تسلسلي / تاريخ ووقت `INV-20260509-143522` / بادئة السنة | ✅ المخزن (`nextNumber`, `previewNumber`) — ⏳ الواجهة |
| 11د | أسماء الملفات: `فاتورة INV-0005 - العميل.pdf`، `عرض سعر QT-0001 - العميل.pdf`، `سند قبض REC-0001 - العميل.pdf`، `كشف حساب - العميل - التاريخ.pdf`، `إشعار تسليم - …` | ✅ منجز |
| 12 | شريط التنقل كما هو؛ مقترح استبدال «الكشوف» — لا يُنفَّذ إلا بموافقة | ⏸ بانتظار القرار |
| 13 | صفحة العميل مرتبة وغير مزدحمة | ✅ منجز |

### تفاصيل التنفيذ
- **models.dart**: `Totals` صحيحة بدون `clamp`؛ `Invoice.quickPhone/deletedAt/isQuick/isDeleted`؛ `Payment/Client.deletedAt`؛ `Org.numberingMode/numberYear` + 9 مفاتيح `show*`.
- **store.dart**: حذف ناعم (`deletedAt`) مع قوائم سلة، `purgeExpiredTrash()` تلقائي عند التشغيل، `restore*/purge*/emptyTrash/daysLeft`، `nextNumber` بثلاثة أنماط، `previewNumber`، `convertQuote(q, clientId:)`، مخطط تصدير 3 مع `counts`، دوال أسماء الملفات الموحّدة.
- **doc_form.dart**: مفتاح «عميل مسجّل / عرض سريع» مع حقلي الاسم والجوال؛ لا يُختار أول عميل تلقائيًا.
- **doc_detail.dart**: تحويل العرض السريع يفتح نموذج عميل مملوءًا مسبقًا ثم يُنشئ الفاتورة؛ صف الضريبة يظهر فقط عند وجود ضريبة؛ الحذف ينقل إلى السلة.
- **clients_screen.dart**: بطاقة رصيد مع شارة «N غير مسدَّدة»، بيانات التواصل مطوية، 4 أزرار بترتيب دورة العمل، أقسام مختصرة، `ClientLedgerScreen` (تبويبات + بحث + تصفية بالحالة).
- **docs_screen.dart**: زر «عرض سعر جديد» لا يشترط وجود عملاء.
- **home_screen.dart / signin_screen.dart**: حسب الملاحظات 1، 2، 5.

### المتبقي للإصدار 2.2.0+4
1. إعادة بناء القائمة الجانبية + مركز الإعدادات + صفحة إعدادات الفواتير (المفاتيح والترقيم بمعاينة حيّة) + حول التطبيق.
2. شاشة سلة المحذوفات.
3. شاشة النسخ الاحتياطية بنمط واتساب.
4. PDF: احترام المفاتيح، إصلاح RTL للجداول، تذييل مقروء، تسمية إشعار التسليم → عرض عينات للمستخدم.
5. رفع رقم الإصدار وبناء APK وإصدار GitHub.
